# deDuplicator-Go Project Plan

## Completed
- [x] Fix deploy script scp destination path issue
  - Changed `/tmp/deduplicator` to `/tmp/deduplicator-binary` to prevent directory creation
  - Updated install command to reference correct binary path
  - Resolves Jenkins deployment failure on rpi4 and other hosts

- [x] Path Grouping Feature Implementation
  - Created database migrations (000005_add_path_groups)
  - Added PathGroup and PathGroupMember types to db package
  - Implemented CRUD functions for path groups
  - Added management commands: group-add, group-list, group-delete, group-add-path, group-remove-path, group-show
  - Implemented cross-host duplicate finding logic
  - Created group-aware deduplication with priority-based retention
  - Added `files dedupe-group` command for cross-host deduplication
  - Supports min/max copy limits and dry-run mode

## Next Steps
- [x] Fix migration test for new path groups migration
- [ ] Test path grouping feature with real data
- [ ] Add unit tests for path group functionality
- [ ] Consider adding capacity-based balancing mode (future enhancement)
